---
name: foreman-supervisor-v1-agent
description: "FOREMAN coded orchestrator (foreman-supervisor-v1) — engine→hybrid gpt-5 router→safety floor→parallel Pattern-A fan-out→risk/gate→action_plan; BUILT & RUN-VERIFIED live MC4 (risk 84 CRITICAL, errors[] empty)"
metadata:
  node_type: memory
  type: project
  originSessionId: 09e22539-e534-4ea8-8e05-462c25c2b1bf
---

`d:\UIpath\foreman-supervisor-v1\main.py` — single LangGraph node `supervise` (uipath-langchain), strict pydantic `GraphInput`(extra=allow, the REAL vision payload) → `GraphOutput`{case,diagnosis,risk,safety_gate,auto_resolve_blocked,invoked[],not_invoked[],action_plan[],summary,errors[]}. Never throws; all failures land in `errors[]`.

**Built from the build-doc's FOUR verbatim blocks** (registry+`g`, `invoke()`, `normalize()`, `risk_score()`). Registry: `FOLDER="Shared/foremen v1"`, `ENGINE="foreman-diagnosis-recommendation"` (user corrected the build-doc's `"foreman-diagnosis"` → the real deployed process name matches the pyproject/folder name); 10 specialists incl. `sla_commercial_impact→"foreman-sla-risk"` (no local folder; pasted by user). **Two extra LLM outputs (phrasing only, never gating):** `risk.rationale` = a one-sentence plain-English explanation of the DETERMINISTIC score (model `gpt-4.1-mini-2025-04-14`, told the number is fixed; code fallback on failure). `worker_message` = a short WhatsApp reply to the worker. **ENGLISH-ONLY** (user pref 2026-06-25 — earlier built it to mirror Hinglish, then reverted: reply must always be plain English regardless of the worker's input language). **Screenshot/WhatsApp layout** the user chose (mirrors the FOREMAN bot screenshot): `Analysis complete for <asset_id>.` / `Root cause: <cause → effect → risk>` / `⚠️ Action: <condensed recommendation, isolation FIRST + "Do NOT touch the live connector">` (NO crew/ETA/dispatch line — user pref 2026-06-25) / `Fleet: <count> sibling strings share crew/batch/lot — crew audit raised` (only if fleet.systemic) / `Step-by-step SOP attached.` (only if report_url minted). **NO filename line** (user pref — the attached PDF shows its own name). Pulls asset/site/root_cause/recommendation/fleet/crew — not just a summary. `_pick_report_blob(diagnosis)` (citations[0]) feeds `report_url`, which is the WhatsApp attachment; worker_message gets `bool(report_url)` to decide the "SOP attached" line. Deterministic fallback reproduces the same layout. ONE emoji only (⚠️ Action). **Both `worker_message` and `risk.rationale` use `UiPathChat(model="gpt-4.1-2025-04-14")`** (full gpt-4.1, NOT mini — user pref). (Runs in the Maestro Case `foremen v1` → Agentic case → **Investigate** stage as process `foreman-supervisor-v1`; the reply is what the downstream `SendWhatsApp` task sends.) Model = **`UiPathChatGoogleGenerativeAI("gemini-2.5-flash")`** (Gemini handles Hindi/Hinglish best; gpt-4.1-mini and even full gpt-4.1 sometimes drift to Hinglish on English input — Gemini reliably mirrors; needs the `vertex` extra, already in pyproject). Both never throw (errors[] + deterministic fallback).

**`report_url` (pre-signed reference PDF):** after assembly, mints a temporary read URL (expiry 20 min) for the relevant PDF in bucket **`foreman-context`** (folder `Shared/foremen v1`). Blob = **`diagnosis.citations[0]`** (engine citations are exact source filenames; user chose "first indexed citation"), fallback `mc4-connector-install-spec.pdf`. Minted THROUGH the authenticated SDK client, NOT raw HTTP (raw urllib hits Cloudflare 1010 — that's what `p.py` documents). This SDK version has **no public get_read_uri**; replicate `buckets.download_async`'s path: `bucket=await sdk.buckets.retrieve_async(name=..., folder_path=FOLDER)` → `spec=sdk.buckets._retrieve_readUri_spec(bucket.id, blob, folder_path=FOLDER)` (endpoint `/orchestrator_/odata/Buckets({id})/UiPath.Server.Configuration.OData.GetReadUri`, params `{path}`, header `x-uipath-folderpath`) → add `params["expiryInMinutes"]=20` → `await sdk.buckets.request_async(spec.method, url=spec.endpoint, params=params, headers=spec.headers)` → `resp.json()["Uri"]`. On any failure report_url="" + errors[] (never throws). Re-ran `uipath init` (output contract changed).

Router/synthesis LLM = `UiPathChat(model="gpt-5-2025-08-07")` — the build-doc's `"gpt-5"` is NOT a valid Gateway id (router 400'd `UiPathBadRequestError`, degraded to all-OFF + safety floor only); real frontier ids from `sdk.agenthub.get_available_llm_models()` are `gpt-5-2025-08-07`/`gpt-5.1-2025-11-13`/`gpt-5.2-2025-12-11`/`gpt-5.4-2026-03-05` (gpt-5* have shouldSkipTemperature+shouldUseMaxCompletionTokens but `.with_structured_output` works on all three tested). Specialists are separate agents on gpt-4.1-mini.

**TWO incidental fixes to the verbatim `invoke()` block (logic kept intact):** (1) `from uipath import UiPath` → `from uipath.platform import UiPath` (only resolvable path this SDK version; matches all sibling agents). (2) `sdk.jobs.extract_output_async(j, folder_path=FOLDER)` → `extract_output_async(j)` — the SDK signature takes **job only**; the extra kwarg raised TypeError swallowed by the block's `except → return {}`, which would have made EVERY successful job yield `{}` and silently break the whole orchestrator. Verified via `inspect.signature`.

**Orchestration (steps 0-7):** normalize → run ENGINE unconditionally first (`{perception,site_id,asset_id,worker_text}`) → hybrid route (LLM proposes per-id {invoke,why} grounded in INVOKE_WHEN; **then a deterministic SAFETY FLOOR in code overrides it**: `engine_safety=="critical"` OR hazard regex [arc/fire/burn/melt/exposed/shock/HV/short/spark/smoke...] in primary_fault/perception/fingerprint → force `safety_compliance` on) → build per-specialist inputs (copies from engine/normalized case) → `asyncio.gather` Pattern-A invokes → `risk_score(engine,safety,fleet,parts,weather,dispatch,history)` + `auto_resolve_blocked = safety.can_block_auto_resolve or safety.safety_critical` → action_plan ordered **SAFETY→PARTS→CREW→WINDOW→FLEET AUDIT** (isolation first) → summary paragraph (notes recurrence + fleet). Specialist output keys used (verified from their main.py): safety{safety_critical,protocol,blockers[],can_block_auto_resolve}, parts{in_stock,location,lead_time_days,substitute,blocks_fix}, dispatch{crew_id,certification_ok,eta_min,route}, weather{earliest_safe_time,safe_window,weather_blockers[],access_constraints[]}, fleet{systemic,affected_count,affected_assets[],recommendation}.

**LOGIC-VERIFIED end-to-end** on the real MC4 payload (`foreman-vision/out.json` → copied to `input.json`) with the network layer mocked (monkeypatch `main.invoke` + `main._route`): risk **84 CRITICAL** (safety 50 + blast_radius 24=8×3 + recurrence 10 — matches the 0.84 in [[foreman-supervisor-invoke-pattern]]), `auto_resolve_blocked=true`, safety floor forced `safety_compliance` even when the mock router omitted it, a `{}` specialist result recorded `foreman-vendor:no output` and continued, action_plan correctly ordered, summary cites the recurrence. `uipath init` + `uipath run agent --file input.json` both succeed structurally.

**RUN-VERIFIED LIVE end-to-end** (after the user re-ran `uipath auth` — the stored refresh_tokens were single-use and already rotated, so the [[foreman-weather-agent]] mint recipe 403'd error 1010; interactive auth was required). Final `out.json` on the real MC4 input: **risk 84 CRITICAL** (safety 50 + blast_radius 24=8×3 + recurrence 10), `auto_resolve_blocked=true`, floor_forced=true; **all 6 jobs ok=true** — safety_compliance, fleet_blast_radius, parts_logistics, field_dispatch, site_access_weather (+engine); 5 correctly NOT invoked with gpt-5's grounded reasons (warranty=workmanship-not-defect, sla=no-tenant-SLA, vendor=no-batch-recall, telemetry=part-already-hard-failed, cost=no-trade-off); action_plan ordered SAFETY(LOTO first)→PARTS(in stock DEL-warehouse)→CREW(CREW-PV-W, ETA 27min)→WINDOW(05:30 IST)→FLEET AUDIT(3 siblings); **`errors[]` empty**. The engine job takes ~1 min (DF+CG+LLM) so a foreground `uipath run` looks like it hangs — run it through or in background; it is NOT stuck.

NOTE: `.env UIPATH_ACCESS_TOKEN` still expires hourly ([[foreman-auth-token-gotcha]]) and this folder has NO `.uipath/.auth.json` fallback, so each fresh session needs `uipath auth` again before a live run.
